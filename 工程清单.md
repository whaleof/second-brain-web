# 第二大脑工作台 · 标准工程清单
> 移动端优先的单页面（SPA）工作台，无后端数据库，数据全部存于浏览器本地（IndexedDB + localStorage 自动备份）。
> 本清单仅保留可完整运行工作台的最新工程文件，已剔除重复备份、旧版单文件包、截图、缓存与死代码。
> 验证日期：2026-07-31（server.py 已接管 8080 端口，新闻/云端同步代理已生效）。

## 一、核心运行文件（必选）

| 文件名 | 文件作用说明 |
|--------|--------------|
| `index.html` | 单页面入口。按 `?v=20260731k` 版本号加载脚本与样式；内联 PWA manifest；移动端优先布局；声明 favicon（`icon.svg` / `icon-192.png` / `icon-512.png`） |
| `styles.css` | 全局样式与两套主题（浅色 / 深色）CSS 变量；含首页 2x2 统计卡、新闻汇总卡、历史归档列表等所有模块样式 |
| `db.js` | 数据持久化核心：IndexedDB 封装（15 个 store）+ localStorage 自动备份 + 跨设备云端同步 + 新闻历史归档（`news_archive`）。含启动恢复与去重导入 |
| `app.js` | 应用主控：路由导航（11 个模块）、主题切换（仅改视觉、零延迟）、设置面板、启动数据完整性检测与自动恢复、Service Worker 注册、清空数据 |
| `sw.js` | Service Worker（网络优先策略，不缓存 JS/CSS，仅作 PWA 离线壳，自动随版本更新） |
| `chart.min.js` | 本地内置的 Chart.js 库（行情图、体重趋势图用；改为本地引用以避免 CDN 不可达） |
| `server.py` | 本地 HTTP 服务器（0.0.0.0:8080，根目录 `/workspace`）+ API 代理：`/api/quote`、`/api/kline`、`/api/fund`、`/api/news`（新浪热搜）、`/api/sync`（云端同步存取） |
| `tunnel.sh` | Cloudflare 隧道启动脚本，用于把本地 8080 服务对外发布（换域名会导致 IndexedDB 看似清空，故配合自动备份/云端同步使用） |

## 二、功能模块（modules/，由 index.html 按序加载）

| 文件名 | 文件作用说明 |
|--------|--------------|
| `modules/home.js` | 首页：2x2 统计卡（待办/已完成/未完成/完成率）+ 今日待办与收件箱（绿色对勾常驻右侧） |
| `modules/plans.js` | 计划：今日任务 / 本周计划 / 长期目标（原 `tasks.js` 任务功能已并入此处） |
| `modules/finance.js` | 记账：账单快速录入与编辑，含金额合法性校验（拦截非正数与 Infinity） |
| `modules/dance.js` | 跳舞训练记录（曲目 / 时长 / 日志） |
| `modules/internship.js` | 工作 / 实习日志 |
| `modules/market.js` | 沪深300 行情：实时报价 + K 线 + Chart.js 走势图（经 `server.py` 代理） |
| `modules/news.js` | 新浪实时热搜：顶部汇总卡（条数 + 刷新 + 往期入口）、5 分钟缓存、后台静默刷新、每日快照自动归档、历史回溯 |
| `modules/memo.js` | 备忘录 |
| `modules/timeline.js` | 时间轴记录 |
| `modules/weight.js` | 体重记录 + 趋势图 |
| `modules/drinks.js` | 饮品记录 |

## 三、图标与资源（assets）

| 文件名 | 文件作用说明 |
|--------|--------------|
| `icon.svg` | 矢量图标 / 网站 favicon |
| `icon-192.png` | PWA 图标 192px（ manifest & 主屏图标） |
| `icon-512.png` | PWA 图标 512px（ manifest 大图） |

## 四、开发 / 部署工具脚本（可选，不参与运行）

| 文件名 | 文件作用说明 |
|--------|--------------|
| `gen_icons.py` | 图标生成工具（由源图生成 PWA 多尺寸图标） |
| `gen_icon_v2.py` | 图标生成工具 v2 |
| `build_single.py` | 构建脚本：把工程打包为单文件 `bundle.html`（当前主力为模块化结构，此脚本属可选历史工具） |

---

## 五、已剔除的冗余 / 重复文件（说明）

| 已删除 | 剔除原因 |
|--------|----------|
| `backup/`（整套旧副本） | 早期重复备份，缺云端同步与自动备份逻辑，已被根目录最新版取代 |
| `bundle.html` | 旧版单文件打包，已被模块化结构取代 |
| `build_single.py` 关联的旧产出 | 同上（脚本本身保留在第四节，便于按需重建单文件版） |
| `modules/tasks.js` | 死代码：从未被 `index.html` 加载，任务功能已由 `plans.js` 承担 |
| `manifest.json` | 未被引用，PWA manifest 已内联进 `index.html` |
| `icon-180.png`、`icon_src.jpg`、`icon_b64.txt`、`rabbit.jpg` | 未使用的图标尺寸与原始素材 |
| `preview*.png`（4 张） | 设计预览截图，非运行所需 |
| `__pycache__/`、`test-results/` | Python 字节码缓存与测试产物 |

## 六、验证结果（2026-07-31）

- `node --check` / `python3 ast` 全量通过（14 个 JS/Python 文件无语法错误）
- `GET /index.html`、`/db.js`、`/sw.js` → HTTP 200
- `GET /api/news` → 返回真实新浪热搜（45 条，含 `update_time`）
- `POST/GET /api/sync?device=xxx` → 上传/下载闭环成功（设备标识校验生效）
- 云端同步目录：`.sync/`（运行时按设备 ID 生成 `*.json`，已自动创建）
